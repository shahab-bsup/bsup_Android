<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bsuperiorsystem.doctor_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Activity.Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.SendResetPasswordRequest.SendResetPasswordRequest" />
        <activity android:name=".Activity.ResetPassword.ResetPasswordActivity" />
        <activity android:name=".Activity.SignUp.SignUpActivity" />
        <activity android:name=".Activity.Welcome.WelcomeActivity" />
        <activity android:name=".Activity.SearchDoctor.SearchActivity" />
        <activity android:name=".Activity.NoDoctorIDPage"></activity>
    </application>

</manifest>